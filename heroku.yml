setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
  config:
      DATABASE_URL: postgres://postgres@db
      RAILS_ENV: production
      NODE_ENV: production
run:
  web: rails s