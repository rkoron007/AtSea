setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile.rails
    frontend: Dockerfile.rails
  config:
      DATABASE_URL: postgres://postgres@db
      RAILS_ENV: production
      NODE_ENV: production
run:
  web: rails s
  frontend: npm run webpack --mode=production