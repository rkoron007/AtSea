setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile.rails
    web: Dockerfile.react
  config:
      DATABASE_URL: postgres://postgres@db
      RAILS_ENV: production
      NODE_ENV: production
run:
  web: rails s