
<% Chat.all.each do | chat | %>
    App[`chats-${<%= chat.id %>}: messages`] = App.cable.subscriptions.create(
      {channel: "MessagesChannel", chat_id: <%= chat.id %>}, {
        received: (data) => data
      }
    );
<% end %>
